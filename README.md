# Template Node.js Express, MongoDb & Pasport-Local

## Environment Variables to configure

Either user Docker-Compose or create a ".env" file with the following content

```
MONGODB_URI="..."
SECRET="..."
```

